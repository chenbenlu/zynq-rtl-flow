# v++ link configuration. Kept in version control because the memory and clock
# bindings it holds are design decisions, not invocation details.
[connectivity]
nk=sparse_conv:1

[clock]
defaultTolerance=0.2
